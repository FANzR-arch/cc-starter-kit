# 你的名字 的 Claude Code 配置

> 这是初始化模板。运行 init.md 后会自动生成个性化版本。
> 也可以直接手动填写。

## 关于我

[在这里写：你是谁、用 CC 主要做什么。一两句话就够。]

示例：我是一名内容创作者，主要用 CC 帮我写文章、整理笔记、管理内容流水线。

## 记忆系统

记忆文件位于 `memory/` 目录，索引见 `memory/MEMORY.md`。
每次对话开始时，优先读取记忆文件了解用户背景。

## 输出规范

- 默认用中文输出
- 回答简洁，不要有多余的铺垫和总结
- [在这里加入你自己的偏好]

## 禁区

- [在这里写 CC 绝对不能做的事，没有就删掉这一节]

## 常用工作流

> 在这里写你的工作流触发规则，格式参考：
> 当用户说「[关键词]」时，按 memory/workflow_main.md 执行。

[示例：当用户说「写文章」时，先读取 memory/workflow_main.md，再按里面的步骤执行。]
